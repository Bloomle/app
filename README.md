# Participant Check

Simple Next.js app for counting event participants with an admin dashboard.

## 🚀 Features
- Three voting options: 1, 2, or 3 participants
- Live total (max 250)
- One vote per user (cookie-based)
- Password-protected admin page
- JSON-based data storage

## ⚙️ Setup

```bash
npm install
cp .env.example .env
npm run dev
